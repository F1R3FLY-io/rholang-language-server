[package]
name = "rholang-language-server"
version = "0.1.0"
edition = "2024"

[dependencies]
anyhow = "1.0"
archery = "1.2.1"  # Matches rpds 1.1.1’s dependency
clap = { version = "4.5", features = ["derive"] }
futures-util = "0.3"
lsp-types = "0.97"
notify = "5.0"
petgraph = "0.8.2"
prost = "0.13"
rholang-parser = { git = "https://github.com/dylon/rholang-rs.git", branch = "dylon/comments" }
rholang-tree-sitter = { git = "https://github.com/dylon/rholang-rs.git", branch = "dylon/comments" }
ropey = "1.6"
rpds = "1.1"
scopeguard = "1.2"
serde_json = "1.0"
serde = { version = "1.0", features = ["derive"] }
thiserror = "1.0"
time = { version = "0.3", features = ["macros", "formatting"] }
tokio-tungstenite = { version = "0.23", optional = true }  # WebSocket support
tokio = { version = "1", features = ["full"] }
tonic = "0.13"
tower-lsp = "0.20"
tracing = "0.1"
tracing-subscriber = { version = "0.3", features = ["env-filter", "time"] }
tree-sitter = "0.25"
url = "2.5"
uuid = { version = "1", features = ["v4"] }
walkdir = "2"

# Platform-specific dependencies
[target.'cfg(unix)'.dependencies]
nix = { version = "0.27", features = ["signal"] }

[dev-dependencies]
indoc = "2"
quickcheck = "1.0"
test_utils = { path = "test_utils" }

[build-dependencies]
tonic-build = "0.13"

[features]
default = ["websocket"]
websocket = ["tokio-tungstenite"]

[[bin]]
name = "dummy_client"
path = "test_utils/src/bin/dummy_client.rs"